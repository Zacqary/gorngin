define([
], function(){
  return {
    audio       : [].concat(app.config.common_sounds),
    map         : [],
    particles   : [],
    spritesheets: app.config.common_sprites.concat([

    ])
  };
});
